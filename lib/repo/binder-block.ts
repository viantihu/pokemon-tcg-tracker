/** BinderBlock: a blocked run of pockets (basic energy or a repurposed duplicate). system-design §4. */
import { createRepo } from "./base";

export const binderBlockRepo = createRepo("binder_block");
