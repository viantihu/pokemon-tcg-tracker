import { Stub } from "../_components/Stub";

export const metadata = { title: "Settings · Binder Ops" };

export default function SettingsPage() {
  return (
    <Stub title="Settings" phase="Coming in M8">
      Binders, pages, pockets, half-split, rainbow order, and the type-to-colour map. Editing the
      map recomputes stored colour bands. The empty Pink band must never be hidden.
    </Stub>
  );
}
