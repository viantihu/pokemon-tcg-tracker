import { Stub } from "../_components/Stub";

export const metadata = { title: "Sync · Binder Ops" };

export default function SyncPage() {
  return (
    <Stub title="Sync" phase="Coming in M9">
      Preview, apply, and undo a Dex CSV reconciliation, plus the unresolved-entry queue. Wraps the
      M4 engine (already at /api/sync).
    </Stub>
  );
}
