"use client";

/**
 * The same boundary one level in, so a screen that throws keeps the app frame (the nav and Sign out) around
 * the message and she can go elsewhere. The (ui) layout itself, and Sign out inside it, fall to app/error.tsx.
 */

import AppError from "../error";

export default AppError;
