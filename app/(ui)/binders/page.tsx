import { Stub } from "../_components/Stub";

export const metadata = { title: "Binders · Binder Ops" };

export default function BindersPage() {
  return (
    <Stub title="Binders & Capacity" phase="Coming in M8">
      Per-section capacity, shelved count, block pockets, placeholders, and free space — answering
      which binder has room for a new Fire line.
    </Stub>
  );
}
